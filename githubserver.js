'use strict';

const express = require('express');

// constants
const PORT = 8080;
const HOST = '0.0.0.0';

// app
const app = exress ();
app.get('/', (req, res) = {
  res.send(h1 style"color:blue:">java home = version 4- running on ecs!!!
}};
        
 app. listen(port, host);
console.log("running on http://$(HOST):(PORT)");
